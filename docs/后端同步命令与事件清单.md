# 后端同步命令与事件清单

## 文档信息

- 当前状态：现状优先，持续维护
- 最近校对：2026-03-27
- 适用范围：authority 真源、`@leafergraph/sync` 根层与 session、以及 editor 当前已接线消费面

## 统计边界

这份文档按“业务领域”整理当前后端同步主线，而不是只统计某一层的 notification。

固定对齐 6 处真源：

- 协议真源：
  [`../openrpc/authority.openrpc.json`](../openrpc/authority.openrpc.json)
- editor authority client 面：
  [`../examples/editor/src/session/graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts)
- editor 当前 document session：
  [`../examples/editor/src/session/graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts)
- sync 根层合同：
  [`../packages/sync/src/core/types.ts`](../packages/sync/src/core/types.ts)
- sync OpenRPC 映射：
  [`../packages/sync/src/openrpc/openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts)
- sync session 恢复链：
  [`../packages/sync/src/session/sync_session.ts`](../packages/sync/src/session/sync_session.ts)

阅读口径固定如下：

- `authority.*` method / notification 是协议真源。
- `@leafergraph/sync` 根层与 session 代表当前协议无关同步面。
- editor 只作为当前已接线消费面出现，不再作为文档命名中心。
- `storage-recovery / authority-event / resync / ack-snapshot` 是 sync 当前真实存在的内部恢复语义，但不是 authority 顶层 method / notification，也不是当前稳定公共载荷字段。

## 文档同步

| 类型 | 名称 / 命令名 | 当前入口 / 层级 | 上游 / 下游对应 | 说明 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `authority method` | `authority.getDocument` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `getDocument?()`；[`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) 的首拉与 `resyncAuthorityDocument()` | 上游是真实 authority 整图拉取；下游落到 `SyncOutlet.getSnapshot()` 与 session 首连 / resync | 主动拉取 authority 当前正式整图快照。 | editor client 面把它写成可选能力；sync 根层把它固化成 outlet 必备能力。 |
| `authority notification` | `authority.document` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `subscribeDocument(...)`；[`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) 的 full snapshot 提交链 | 下游落到 `SyncOutletEvent: snapshot`、`SyncSession.subscribeDocument(...)` | authority 主动回推整图权威快照。 | 默认模板在没有连接级 baseline 或 diff 不可安全生成时发送 full snapshot。 |
| `authority notification` | `authority.documentDiff` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `subscribeDocumentDiff(...)`；[`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) 的 diff 应用链 | 下游落到 `SyncOutletEvent: patch`、session patch 前推与失败后 `resync()` | authority 主动回推增量文档 diff。 | editor 与 sync 当前都采用 `resync-only`，diff 失配时直接回拉 full snapshot。 |
| `authority method` | `authority.submitOperation` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `submitOperation(operation, context)`；[`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) 的 `submitOperationWithAuthority(...)` | sync 根层对应 `SyncCommand: document.apply-operation` 与 `SyncAck: document.apply-operation` | 向 authority 提交正式图操作，并等待 authority 确认结果。 | OpenRPC 子出口会把 sync command 映射回 `operation + context(currentDocument, pendingOperationIds)`。 |
| `authority method` | `authority.replaceDocument` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `replaceDocument?(document, context)`；[`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) 的 `replaceDocument(...)` | sync 根层对应 `SyncCommand: document.replace` 与 `SyncAck: document.replace` | 以整图方式替换 authority 当前文档。 | editor client 面是可选能力；当前 OpenRPC transport client 已实现。 |
| `sync 根层能力` | `SyncOutlet.getSnapshot()` | [`types.ts`](../packages/sync/src/core/types.ts) 的 `SyncOutlet`；[`openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts) 负责映射 | 上游对应 `authority.getDocument`；下游被 `SyncSession.connect()` 与 `SyncSession.resync()` 调用 | 根层保留的独立整图拉取入口。 | 当前明确不伪装成特殊 command。 |
| `sync 根层命令` | `SyncCommand: document.apply-operation` | [`types.ts`](../packages/sync/src/core/types.ts)；通过 `SyncSession.submitCommand(...)` 提交 | 上游映射到 `authority.submitOperation`；下游与 editor 的 `submitOperationWithAuthority(...)` 语义相近 | 协议无关的正式图操作命令。 | 当前根层固定带 `commandId`、`issuedAt` 与 `operation`。 |
| `sync 根层命令` | `SyncCommand: document.replace` | [`types.ts`](../packages/sync/src/core/types.ts)；通过 `SyncSession.submitCommand(...)` 提交 | 上游映射到 `authority.replaceDocument`；下游与 editor 的 `replaceDocument(...)` 语义对齐 | 协议无关的整图替换命令。 | 当前根层固定承载 `snapshot`。 |
| `sync 根层事件` | `SyncOutletEvent: snapshot` | [`types.ts`](../packages/sync/src/core/types.ts)；[`openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts) 产出 | 上游通常来自 `authority.document`；下游进入 `SyncSession.subscribeDocument(...)` | 根层统一的整图快照事件。 | 也是 authority 首快照、外部替换后 full push 的统一投影面。 |
| `sync 根层事件` | `SyncOutletEvent: patch` | [`types.ts`](../packages/sync/src/core/types.ts)；[`openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts) 产出 | 上游对应 `authority.documentDiff`；下游由 session 尝试前推并在失败时 `resync()` | 根层统一的文档增量事件。 | 当前不在根层定义 merge / replay，只支持可信基线上的 patch 前推。 |
| `sync session` | `SyncSession.getDocumentSnapshot()` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游来自 `storage-recovery`、`authority-event`、`resync`、`ack-snapshot` 应用后的当前快照；下游供业务层即时读取 | 读取当前 session 持有的文档事实视图。 | 若尚未建立任何基线，则返回 `undefined`。 |
| `sync session` | `SyncSession.subscribeDocument(...)` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游承接 recovery 基线、authority snapshot、patch 前推后的结果；下游是业务层 / 页面订阅点 | 订阅当前文档事实视图。 | 当前允许订阅者先收到 `storage-recovery` 临时基线，再收到 authority 首快照整体替换。 |
| `sync ack` | `SyncAck: document.apply-operation` | [`types.ts`](../packages/sync/src/core/types.ts)；由 `SyncSession.submitCommand(...)` 返回 | 上游对应 `authority.submitOperation` result；下游可映射到 editor 的 operation confirmation 语义 | authority 对文档操作命令的最小确认结构。 | 当前状态固定为 `accepted / rejected / resync-required`；可带 `changed`、`documentRevision`、`snapshot`。 |
| `sync ack` | `SyncAck: document.replace` | [`types.ts`](../packages/sync/src/core/types.ts)；由 `SyncSession.submitCommand(...)` 返回 | 上游对应 `authority.replaceDocument` result；下游可触发 `ack-snapshot` 或继续保留当前整图 | authority 对整图替换命令的最小确认结构。 | OpenRPC 当前成功解码时会回 accepted 结果；根层类型本身仍沿用统一 `SyncDocumentAck`。 |

## 运行时同步

| 类型 | 名称 / 命令名 | 当前入口 / 层级 | 上游 / 下游对应 | 说明 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `authority method` | `authority.controlRuntime / node.play` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `controlRuntime({ type: "node.play", nodeId })` | sync 根层对应 `SyncCommand: runtime.control` 与 `SyncAck: runtime.control` | 请求 authority 运行单节点。 | 当前不为 `node.play` 单独拆新命令。 |
| `authority method` | `authority.controlRuntime / graph.play` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `controlRuntime({ type: "graph.play" })` | sync 根层对应 `SyncCommand: runtime.control` 与 `SyncAck: runtime.control` | 请求 authority 启动整图运行。 | 后续运行进度不靠 ack 增量表达，而是继续走 runtime feedback 与文档回推。 |
| `authority method` | `authority.controlRuntime / graph.step` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `controlRuntime({ type: "graph.step" })` | sync 根层对应 `SyncCommand: runtime.control` 与 `SyncAck: runtime.control` | 请求 authority 执行一次单步推进。 | 当前默认模板已覆盖该请求与对应反馈链。 |
| `authority method` | `authority.controlRuntime / graph.stop` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `controlRuntime({ type: "graph.stop" })` | sync 根层对应 `SyncCommand: runtime.control` 与 `SyncAck: runtime.control` | 请求 authority 停止当前图运行。 | 当前默认模板已覆盖停止语义与停止后的 runtime feedback。 |
| `authority notification` | `authority.runtimeFeedback` | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 通过 `EditorRuntimeFeedbackInlet.subscribe(...)` 接入；[`graph_document_authority_transport.ts`](../examples/editor/src/session/graph_document_authority_transport.ts) 把 transport 事件转给 runtime feedback 监听器 | 下游落到 `SyncOutletEvent: feedback` 与 `SyncSession.subscribeRuntimeFeedback(...)` | authority 主动回推统一运行反馈。 | payload 内部当前子类型只有 `graph.execution / node.execution / node.state / link.propagation`，它们不是新的顶层 notification。 |
| `sync 根层命令` | `SyncCommand: runtime.control` | [`types.ts`](../packages/sync/src/core/types.ts)；通过 `SyncSession.submitCommand(...)` 提交 | 上游映射到 `authority.controlRuntime`；下游与 editor 的 `controlRuntime(...)` 语义对齐 | 协议无关的 runtime 控制命令。 | 当前根层固定承载 `node.play / graph.play / graph.step / graph.stop` 四类请求。 |
| `sync 根层事件` | `SyncOutletEvent: feedback` | [`types.ts`](../packages/sync/src/core/types.ts)；[`openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts) 产出 | 上游对应 `authority.runtimeFeedback`；下游进入 `SyncSession.subscribeRuntimeFeedback(...)` | 根层统一的运行反馈事件。 | 当前不把 presence / awareness 混进这条事件面。 |
| `sync session` | `SyncSession.subscribeRuntimeFeedback(...)` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游承接 `SyncOutletEvent: feedback`；下游供业务层 / 页面订阅 | 订阅当前会话收到的 runtime feedback。 | 当前只做透传，不做 storage 恢复。 |
| `sync ack` | `SyncAck: runtime.control` | [`types.ts`](../packages/sync/src/core/types.ts)；由 `SyncSession.submitCommand(...)` 返回 | 上游对应 `authority.controlRuntime` result；下游可映射到 editor 当前运行控制结果 | authority 对 runtime 控制命令的最小确认结构。 | 当前可带 `changed` 与 `runtimeState`，状态仍是 `accepted / rejected / resync-required`。 |

## 连接、错误与恢复

| 类型 | 名称 / 命令名 | 当前入口 / 层级 | 上游 / 下游对应 | 说明 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `editor transport / client 状态` | connection status | [`graph_document_authority_client.ts`](../examples/editor/src/session/graph_document_authority_client.ts) 的 `getConnectionStatus()` / `subscribeConnectionStatus(...)`；[`graph_document_authority_transport.ts`](../examples/editor/src/session/graph_document_authority_transport.ts) 负责转接 | 上游来自 transport / WebSocket 状态；下游供 editor 当前页面与 session 使用 | editor 当前 authority 连接状态面。 | 这不是 `authority.openrpc.json` 里的 method / notification。 |
| `sync 根层事件` | `SyncOutletEvent: connection` | [`types.ts`](../packages/sync/src/core/types.ts)；由 outlet / carrier 状态变化产出 | 上游来自 carrier 连接状态；下游进入 `SyncSession.getConnectionStatus()` 与 `subscribeConnectionStatus(...)` | 根层统一的连接状态事件。 | 当前状态集合是 `idle / connecting / connected / disconnected / reconnecting`。 |
| `sync session` | `SyncSession.getConnectionStatus()` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游承接 `SyncOutletEvent: connection`；下游供业务层即时读取 | 读取当前 session 连接状态。 | 不承载 authority 文档事实，只反映链路状态。 |
| `sync session` | `SyncSession.subscribeConnectionStatus(...)` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游承接 `SyncOutletEvent: connection`；下游供业务层订阅 | 订阅当前 session 连接状态变化。 | 订阅时会立即回放当前状态。 |
| `sync 根层事件` | `SyncOutletEvent: error` | [`types.ts`](../packages/sync/src/core/types.ts)；[`openrpc_outlet.ts`](../packages/sync/src/openrpc/openrpc_outlet.ts) 负责产生 | 上游来自 decode / transport / protocol 错误；下游由 session 映射到恢复策略 | 根层统一的错误事件。 | 这不是 authority 顶层 notification，而是 outlet 对协议 / 传输失败的公开映射。 |
| `sync 根层错误分类` | `SyncOutletError.kind: decode / transport / protocol` | [`types.ts`](../packages/sync/src/core/types.ts) | 上游是 outlet 内部错误来源；下游影响 `SyncSession` 的恢复分支 | sync 根层固定错误分类。 | `decode / protocol` 当前会触发 `fail-and-refetch`；`transport` 只交给连接状态链处理。 |
| `sync session` | `SyncSession.connect()` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游串起 storage 解析、outlet 订阅、`getSnapshot()`；下游建立首个 authority 事实链 | 建立同步会话并完成首连恢复链。 | 当前时序固定为“先 recovery，后 authority 首快照整体替换”。 |
| `sync session` | `SyncSession.resync()` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游通常由 ack rejection、patch failure、reconnect gap、decode error 触发；下游走 `getSnapshot()` 整图重拉 | 强制整图重同步入口。 | v1 固定为 `authority-first + resync-only`，不做 pending replay。 |
| `sync 策略字段` | `ResyncPolicy.onAckRejected` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游对应 `SyncAck.status = rejected / resync-required`；下游触发 `SyncSession.resync()` | 规定命令确认失败后的恢复策略。 | 默认值固定为 `refetch`。 |
| `sync 策略字段` | `ResyncPolicy.onPatchFailure` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游对应 patch 前推失败；下游触发 `SyncSession.resync()` | 规定 patch 失配后的恢复策略。 | 默认值固定为 `refetch`。 |
| `sync 策略字段` | `ResyncPolicy.onReconnect` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游对应已建立 authority 事实后的 reconnect gap；下游触发 `SyncSession.resync()` | 规定断线恢复后的整图对齐策略。 | 默认值固定为 `refetch`。 |
| `sync 策略字段` | `ResyncPolicy.onDecodeError` | [`types.ts`](../packages/sync/src/core/types.ts) 与 [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) | 上游对应 `SyncOutletEvent:error` 的 `decode / protocol`；下游触发 `SyncSession.resync()` | 规定解码或协议错误后的恢复策略。 | 默认值固定为 `fail-and-refetch`。 |
| `sync 内部恢复语义` | `storage-recovery` | [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) 的 `SnapshotSource` | 上游来自 `SyncStorage.load()` 读出的快照；下游先进入 `SyncSession.subscribeDocument(...)` | 启动期临时恢复基线来源。 | 当前实现真实存在，但不是 authority 顶层 method / notification，也不是公开载荷字段。 |
| `sync 内部恢复语义` | `authority-event` | [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) 的 `SnapshotSource` | 上游来自 `SyncOutletEvent: snapshot` 与 patch 前推成功；下游更新当前 authority 基线 | 正式 authority 事件写入当前基线时的内部来源标签。 | 当前实现真实存在，但不是公开 API 字段。 |
| `sync 内部恢复语义` | `resync` | [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) 的 `SnapshotSource` | 上游来自 `SyncSession.resync()` 的整图拉取；下游整体替换当前基线 | 重同步快照来源标签。 | 当前实现真实存在，但不是 authority 顶层 notification。 |
| `sync 内部恢复语义` | `ack-snapshot` | [`sync_session.ts`](../packages/sync/src/session/sync_session.ts) 的 `SnapshotSource` | 上游来自 `SyncAck.snapshot`；下游整体替换当前基线 | authority 在 ack 中直接回整图时的内部来源标签。 | 当前实现真实存在，但不是公开载荷字段。 |
| `sync 存储元信息` | `SyncStoredState.recoveryMeta` | [`types.ts`](../packages/sync/src/core/types.ts)；[`sync_session.ts`](../packages/sync/src/session/sync_session.ts) 保存；[`src/storage/README.md`](../packages/sync/src/storage/README.md) 说明 | 上游来自 authority 已确认快照落盘；下游为恢复链提供最小元信息 | recovery 资料里的最小元信息。 | 当前只承载 `revision?` 与 `savedAt?`，不扩写成新的事件协议。 |

## 前端 bundle 同步

| 类型 | 名称 / 命令名 | 当前入口 / 层级 | 上游 / 下游对应 | 说明 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `authority notification` | `authority.frontendBundlesSync` | [`graph_document_authority_transport.ts`](../examples/editor/src/session/graph_document_authority_transport.ts) 的 `subscribeFrontendBundles(...)`；[`shell/provider.tsx`](../examples/editor/src/shell/provider.tsx) 消费目录同步事件 | 下游只落到 editor 当前 bundle 链；`@leafergraph/sync` 根层无对应 | authority 主动回推前端 bundle 目录快照与增量事件。 | 事件体模式为 `full / upsert / remove`；默认 Python 模板在连接建立后会先发一份 `full` snapshot。 |
| `editor 当前消费面` | `subscribeFrontendBundles(...)` | [`graph_document_authority_transport.ts`](../examples/editor/src/session/graph_document_authority_transport.ts) | 上游对应 `authority.frontendBundlesSync`；下游供 shell / provider 消费 | editor 当前接线的前端 bundle 订阅入口。 | OpenRPC 子出口当前只校验 `authority.frontendBundlesSync` 结构，然后忽略，不把它投影进 `SyncOutletEvent`。 |

## 不纳入主线但容易混淆的配套接口

| 类型 | 名称 / 命令名 | 当前入口 / 层级 | 上游 / 下游对应 | 说明 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `protocol method` | `rpc.discover` | [`authority_openrpc/runtime.ts`](../examples/editor/src/session/authority_openrpc/runtime.ts) 的协议适配与生成代码 | 上游是协议自描述；下游服务调试、生成与一致性检查 | 返回 authority 当前 OpenRPC 文档。 | 它不是文档同步或运行时同步主线，也不会映射成 sync 根层命令。 |
| `editor session 内部` | `subscribePending(...)` | [`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) | 无 authority 真源；无 sync 根层对应 | 暴露 editor 本地 pending 操作队列变化。 | 它描述的是本地编排面，不是后端主动同步事件。 |
| `editor session 内部` | `subscribeProjection(...)` | [`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) | 无 authority 真源；无 sync 根层对应 | 暴露 viewport 用的内部文档投影事件，区分 `full / diff`。 | 它描述的是 editor 内部投影链，而不是 authority protocol 事件。 |
| `editor session 内部` | `subscribeOperationConfirmation(...)` | [`graph_document_session.ts`](../examples/editor/src/session/graph_document_session.ts) | 无 authority 真源；与 `SyncAck` 语义相近但不是同一层公开合同 | 暴露 editor 对已提交操作的异步 authority 确认。 | 它不是后端主动 notification，而是 session 对 request/response 的本地整理面。 |

## 当前贯通状态总表

| 归类 | 能力 | 当前落点 | 说明 |
| :--- | :--- | :--- | :--- |
| `authority + sync + editor 已贯通` | `authority.getDocument` | `SyncOutlet.getSnapshot()`，editor `getDocument?()` / `resyncAuthorityDocument()` | authority 真源、sync 根层与 editor 当前消费链都已有明确落点。 |
| `authority + sync + editor 已贯通` | `authority.document` | `SyncOutletEvent: snapshot`，editor `subscribeDocument(...)` | 整图权威快照通道已贯通。 |
| `authority + sync + editor 已贯通` | `authority.documentDiff` | `SyncOutletEvent: patch`，editor `subscribeDocumentDiff(...)` | 文档 diff 通道已贯通，但当前统一采用 `resync-only` 兜底。 |
| `authority + sync + editor 已贯通` | `authority.submitOperation` | `SyncCommand: document.apply-operation` / `SyncAck`，editor `submitOperationWithAuthority(...)` | 正式图操作提交链已贯通。 |
| `authority + sync + editor 已贯通` | `authority.replaceDocument` | `SyncCommand: document.replace` / `SyncAck`，editor `replaceDocument(...)` | 整图替换链已贯通。 |
| `authority + sync + editor 已贯通` | `authority.controlRuntime` | `SyncCommand: runtime.control` / `SyncAck`，editor `controlRuntime(...)` | runtime 控制请求链已贯通。 |
| `authority + sync + editor 已贯通` | `authority.runtimeFeedback` | `SyncOutletEvent: feedback`，editor runtime feedback inlet | 统一运行反馈链已贯通。 |
| `sync 专属` | `connection` | `SyncOutletEvent: connection`，`SyncSession.getConnectionStatus()`，`subscribeConnectionStatus(...)` | 这是 sync / transport 层的公开状态面，不是 authority 顶层 notification。 |
| `sync 专属` | `error` | `SyncOutletEvent: error`，`SyncOutletError.kind` | 这是 sync / outlet 对协议与传输错误的公开映射。 |
| `sync 专属` | `ack` | `SyncAck: document.apply-operation / document.replace / runtime.control` | ack 是 sync 根层公开确认结构，不是 authority notification。 |
| `sync 专属` | `session 订阅面` | `SyncSession.getDocumentSnapshot()`、`subscribeDocument(...)`、`subscribeRuntimeFeedback(...)` | 这些是 sync 当前稳定公开会话面。 |
| `sync 专属` | `resync 与恢复语义` | `SyncSession.connect()`、`SyncSession.resync()`、`ResyncPolicy.*`、`storage-recovery / authority-event / resync / ack-snapshot` | 这些能力真实存在，但其中内部来源标签不是公开载荷字段。 |
| `editor 专属` | `subscribePending(...)` | editor document session | 只描述 editor 本地 pending 队列。 |
| `editor 专属` | `subscribeProjection(...)` | editor document session | 只描述 editor 本地投影链。 |
| `editor 专属` | `subscribeOperationConfirmation(...)` | editor document session | 只描述 editor 对 request/response 的本地整理结果。 |
| `协议专属` | `authority.frontendBundlesSync` | authority notification，editor `subscribeFrontendBundles(...)`，OpenRPC 子出口校验后忽略 | 当前只在 authority / editor bundle 链里生效，不进入 sync 根层。 |
| `协议专属` | `rpc.discover` | OpenRPC 协议适配、代码生成与调试工具链 | 属于协议自描述，不是同步主线。 |

## 当前结论

截至当前仓库状态，后端同步主线已经可以按 5 类能力收口：

- 文档同步：authority 整图、diff、操作提交与整图替换，以及 sync 根层的 `getSnapshot()`、`SyncCommand`、`SyncAck`、`subscribeDocument(...)`
- 运行时同步：authority `controlRuntime` 与 `runtimeFeedback`，以及 sync 根层的 `runtime.control`、`feedback`、`subscribeRuntimeFeedback(...)`
- 连接、错误与恢复：sync 当前公开的连接状态、错误事件、重同步策略与内部恢复语义
- 前端 bundle 同步：`authority.frontendBundlesSync` 与 editor 当前 bundle 订阅链
- 配套接口：`rpc.discover` 和 editor session 的本地 pending / projection / confirmation

其中，authority 的文档与运行时主线已经和 `@leafergraph/sync`、editor 当前消费面形成三层贯通；`frontendBundlesSync` 仍然停留在协议 / editor 专属链路里；`storage-recovery / authority-event / resync / ack-snapshot` 则只应被理解为 sync 当前实现里的内部恢复语义，而不是新的公开协议字段。

## 对比主流方案后建议补全的 API

这一节只讨论“建议补齐的 API 缺口”，不是当前已经实现或已经导出的能力。

当前基线固定对齐：

- [后端同步命令与事件清单.md](./后端同步命令与事件清单.md)
- [types.ts](../packages/sync/src/core/types.ts)
- [README.md](../packages/sync/README.md)

对标口径固定参考 4 类主流方案：

- Yjs
- Automerge Repo
- Liveblocks
- Replicache

这里的建议仍以 `authority-first + resync-only` 为前提，只补当前路线下最缺、最容易和主流方案拉齐的 API 面；不会把 local-first / CRDT 路线误写成当前目标。

### 近期建议补齐

| 优先级 | 建议 API | 建议层级 | 对标主流能力 | 解决的当前缺口 | 建议状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `P0` | `SyncStatus`<br/>`SyncSession.getSyncStatus()`<br/>`SyncSession.subscribeSyncStatus(...)` | sync 根层 / session | Yjs 的 synced / sync-status，Automerge Repo 的 ready 语义，Liveblocks 的 storage/sync status | 当前只能看到 `ConnectionStatus`，无法区分“正在读 `storage-recovery` 临时基线”和“已经拿到 authority 首快照并建立正式事实链” | 建议新增独立 `SyncStatus`，建议状态集合固定为 `idle / recovering / syncing / synced / stale / error`。它要和 `storage-recovery / authority-event / resync / ack-snapshot` 这些内部恢复语义对齐，但不直接把这些内部标签暴露成公共载荷字段。 |
| `P1` | `SyncSession.disconnect()`<br/>`SyncSession.reconnect()`<br/>`SyncOutlet.disconnect?()`<br/>`SyncOutlet.reconnect?()` | session + outlet | Yjs provider 的 connect / disconnect，Liveblocks 的 reconnect / disconnect | 当前只有 `connect()` 与 `dispose()`，没有手动断开、显式重连和“先断线再重连”的控制面 | 建议作为连接生命周期 API 单独补齐；它补的是人为控制链路，不替代现有 `resync()`。 |
| `P1` | 扩展 `SyncOutletError`<br/>新增 `code`、`recoverable` | sync 根层 | Liveblocks 的 lost-connection / error 分流，主流 transport 层的 recoverable error 语义 | 当前只有 `kind: decode / transport / protocol`，不足以支撑鉴权失败、权限不足、超时和 UI 恢复分流 | 建议 `code` 值固定写成 `auth / permission / timeout / transport / protocol / decode`；`recoverable` 用于区分“可重试”与“需要人工处理”的错误，不改变现有 `kind` 的顶层分组。 |

### 建议作为扩展层

| 优先级 | 建议 API | 建议层级 | 对标主流能力 | 解决的当前缺口 | 建议状态 / 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `候选` | `updatePresence()`<br/>`subscribePresence(...)` | app-level extension 或独立子面 | Yjs Awareness，Liveblocks Presence / Others | 当前缺少多人在线状态、光标、选区、用户态等临时协作信息通道 | 适合作为独立子面；不要塞进 `RuntimeFeedback`，也不要混进 authority 文档快照。 |
| `候选` | `broadcastEvent()`<br/>`subscribeEvent(...)` | outlet / protocol 或 app-level extension | Liveblocks broadcast event，Automerge Repo 的 ephemeral message | 当前缺少“只传临时 UI 信号、不落文档、不进恢复链”的轻量事件面 | 只承载临时 UI 信号，如 hover、toast、临时面板状态；不要进入文档快照、recovery 或 `RuntimeFeedback`。 |
| `候选` | `subscribeSelector(...)` 或 `subscribeNode(...)` | session | Liveblocks 的细粒度订阅，Replicache 的 query / subscription 思路 | 当前只有整图 `subscribeDocument(...)`，大图场景下缺少更细粒度的读取与刷新控制 | 建议用于性能优化，不替代现有整图订阅；具体 selector 语义留到实现期再收口。 |
| `候选` | `SyncClient` 或 `SyncRepo` | sync 上位管理层 | Automerge Repo 的 Repo，Liveblocks client / room 管理面 | 当前边界固定是一份文档一个 session，缺少多 session、多 authority、共享 transport / auth / storage 的管理层 | 建议作为上位管理器，而不是扩进当前单 session 合同；命名可在实现时二选一，但文档阶段先保留两个候选名。 |

### 当前明确不纳入本项目主线

| 能力 | 主流方案里常见点 | 当前为何不纳入 | 当前状态 / 备注 |
| :--- | :--- | :--- | :--- |
| `optimistic mutation` | Replicache、部分 CRDT / local-first 方案会先本地投影再异步对齐服务端 | 与当前 [README.md](../packages/sync/README.md) 已写死的 `authority-first + resync-only` 不一致 | 本轮只作为边界说明，不作为建议补齐项。 |
| `pending queue introspection` | 主流 local-first 方案常暴露待上传 mutation、队列长度或 pending 状态 | 当前 sync 明确不承诺跨重启 pending replay，也不以 pending 队列作为公开根层语义 | 本轮只作为边界说明，不作为建议补齐项。 |
| `rollback / rebase` | local-first / CRDT 方案常需要回滚本地投影并在新基线上重放 | 当前 sync 遇到 ack rejection、patch failure 或 decode error 时统一走 `resync()` | 本轮只作为边界说明，不作为建议补齐项。 |
| `offline replay` | 主流离线优先方案常支持离线提交、上线后补推与重放 | 当前 sync 只把 storage 用作恢复基线，不把它当成离线命令队列 | 本轮只作为边界说明，不作为建议补齐项。 |
| `CRDT merge layer` | Yjs、Automerge 等方案把 merge 语义内建为核心能力 | 当前 sync 以 authority 权威快照和 `resync-only` 为准，不以内建 CRDT merge 为目标 | 本轮只作为边界说明，不作为建议补齐项。 |

### API 补全优先级总表

| 归类 | 代表能力 | 说明 |
| :--- | :--- | :--- |
| `优先进入 sync 根层 / session` | `SyncStatus`、`getSyncStatus()`、`subscribeSyncStatus(...)`、`disconnect()`、`reconnect()`、扩展 `SyncOutletError` | 这组能力最贴近当前 authority-first 路线，能直接补齐“authority-ready 可见性”“连接生命周期控制”“错误恢复分流”三类缺口。 |
| `建议作为协议或应用扩展` | `updatePresence()`、`subscribePresence(...)`、`broadcastEvent()`、`subscribeEvent(...)`、`subscribeSelector(...)`、`subscribeNode(...)`、`SyncClient` / `SyncRepo` | 这组能力与主流协作体验高度相关，但更适合作为独立子面、协议扩展或上位管理层，不应直接混进当前根层文档事实链。 |
| `当前保留为非目标` | `optimistic mutation`、`pending queue introspection`、`rollback / rebase`、`offline replay`、`CRDT merge layer` | 这组能力在主流方案中常见，但和当前 `authority-first + resync-only` 边界冲突；文档应明确它们是已知方向，不是当前主线。 |
