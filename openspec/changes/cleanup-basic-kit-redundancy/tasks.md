## Tasks

- [x] Add characterization tests for the `basic-kit` package surface, widget lifecycle behavior, and cleanup semantics.
- [x] Extract shared focus, press, render, and primary-key helpers in `packages/basic-kit/src/widget/template.ts`.
- [x] Update each widget controller to use the shared helpers while keeping widget-specific logic unchanged.
- [x] Run `bun run test:basic-kit` and `bun run build:basic-kit`, then fix any regressions.
