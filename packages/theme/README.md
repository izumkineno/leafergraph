# `@leafergraph/theme`

`@leafergraph/theme` 负责承载 LeaferGraph workspace 的视觉主题真源：

- `theme mode`
- 命名主题 preset 注册表
- 默认 Widget 主题
- 默认 graph 主题
- 默认 context-menu 主题

这个包只负责主题数据、token 和 preset 解析，不承载运行时场景刷新宿主。
